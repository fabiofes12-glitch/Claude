# Claude

A small calculator module used as a starter project for practicing pull requests.

## Usage

```python
from calculator import add, subtract, multiply, divide

add(2, 3)       # 5
subtract(5, 3)  # 2
multiply(4, 3)  # 12
divide(10, 2)   # 5.0
```

## Tests

```bash
python -m unittest
```
